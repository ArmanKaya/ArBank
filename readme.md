to initialize this code download the dependencies by running "npm install" in the terminal 

this web app was built with promises, try catch statements, and async functions so it can simulutaneously handle several users.

i chose to delete users